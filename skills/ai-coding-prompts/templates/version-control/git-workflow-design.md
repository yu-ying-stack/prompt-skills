# Git 工作流设计 Prompt 模板

## 元信息

| 字段 | 值 |
| ------ | ----- |
| 模板ID | VCS-WORKFLOW-001 |
| 版本 | 1.0 |
| 分类 | 版本控制 / Git 工作流 |
| 适用场景 | 团队协作分支策略、发布流、热修复流、代码审查流程 |
| 创建日期 | 2026-06-08 |
| 作者 | AI Coding Prompt 模板库 |

## 角色定位

你是一位资深研发流程顾问，擅长为不同规模团队设计 Git 分支模型、PR 审查规则、发布流和权限控制策略，平衡交付效率与变更安全。

## 输入参数

| 参数名 | 类型 | 必填 | 说明 |
| -------- | ------ | ------ | ------ |
| TEAM_MODEL | String | 是 | 团队规模、角色与协作模式 |
| RELEASE_CADENCE | String | 是 | 发布频率与版本策略 |
| REPOSITORY_STRUCTURE | String | 是 | 单仓、多仓或 Monorepo |
| QUALITY_CONTROLS | String | 是 | 代码审查、CI 状态、保护分支要求 |
| HOTFIX_REQUIREMENTS | String | 是 | 紧急修复流程要求 |
| COMPLIANCE_RULES | String | 否 | 审计、签名、权限、合并策略要求 |

## Prompt 正文

请基于以下信息，设计一套 Git 工作流，并说明分支模型、合并策略和治理规则。

**团队模型：** {{TEAM_MODEL}}
**发布节奏：** {{RELEASE_CADENCE}}
**仓库结构：** {{REPOSITORY_STRUCTURE}}
**质量控制：** {{QUALITY_CONTROLS}}
**热修复要求：** {{HOTFIX_REQUIREMENTS}}
**合规规则：** {{COMPLIANCE_RULES}}

请完成以下任务：

1. 选择合适的分支模型并说明适用原因。
2. 定义 feature、release、hotfix、main 等分支职责与生命周期。
3. 说明 PR 规则、代码审查门槛、CI 准入、保护分支策略。
4. 设计发布、回滚、cherry-pick、版本回溯和审计机制。
5. 给出团队推广与落地建议。

## 约束条件

1. 必须根据团队协作模式和发布节奏选择工作流，不得照搬固定模板。
2. 必须定义分支职责、合并方向和删除规则，禁止产生长期无人治理的分支。
3. 必须把 CI 状态、代码审查和保护分支纳入准入规则，禁止绕过关键质量门槛直接合并。
4. 必须定义热修复与版本回溯流程，禁止在紧急变更时临时拍脑袋处理。
5. 必须说明权限和审计要求，禁止让关键分支缺乏最小权限控制。

## 输出要求

1. 输出推荐工作流与选择理由。
2. 输出分支模型和生命周期说明。
3. 输出 PR、CI、审查与保护规则。
4. 输出发布和热修复流程。
5. 输出落地步骤与风险提醒。

6. 至少输出 1 个矩阵表、1 个流程图（可用 Mermaid 或等价文本流程）和 1 个风险清单（含风险、影响、缓解措施）。

## 输出格式

```markdown
# Git 工作流方案

## 1. 方案概述

## 2. 分支模型
| 分支 | 作用 | 创建方式 | 合并方向 | 删除规则 |
| ------ | ------ | ---------- | ---------- | ---------- |`r`n`r`n## 3. PR 与质量门禁

## 4. 发布与热修复流程

## 5. 推广与治理建议

## 附录 A. 分支与变更治理矩阵

| 对象 | 规则 | 触发条件 | 自动化校验 | 例外处理 |
| ------ | ------ | ------ | ------ | ------ |

## 附录 B. 流程图

（使用 Mermaid 或等价文本流程，至少包含：创建分支/提交 -> PR 校验 -> 合并发布 -> 热修或回滚）

## 附录 C. 风险清单

| 风险 | 触发条件 | 影响 | 治理措施 | 审计方式 |
| ------ | ------ | ------ | ------ | ------ |

## 自检清单

- [ ] 是否根据团队与发布节奏选择了工作流
- [ ] 是否定义了清晰分支职责和生命周期
- [ ] 是否纳入了 PR、CI 和保护分支规则
- [ ] 是否给出了热修复与回溯流程
- [ ] 是否说明了权限与审计控制
