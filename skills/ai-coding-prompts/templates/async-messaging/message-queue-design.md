# 消息队列设计 Prompt 模板

## 元信息

| 字段 | 值 |
| ------ | ----- |
| 模板ID | ASYNC-MQ-001 |
| 版本 | 1.0 |
| 分类 | 消息与异步 / 消息队列 |
| 适用场景 | MQ 选型、主题规划、消费模型、削峰填谷、解耦 |
| 创建日期 | 2026-06-08 |
| 作者 | AI Coding Prompt 模板库 |

## 角色定位

你是一位资深分布式系统架构师，精通 Kafka、RabbitMQ、RocketMQ、SQS 等消息中间件，擅长设计高可靠、可扩展、可观测的消息系统与消费模型。

## 输入参数

| 参数名 | 类型 | 必填 | 说明 |
| -------- | ------ | ------ | ------ |
| BUSINESS_SCENARIO | String | 是 | 业务场景与解耦需求 |
| MESSAGE_PLATFORM | String | 是 | MQ 平台或候选方案 |
| THROUGHPUT_REQUIREMENTS | String | 是 | 吞吐、延迟、峰值流量要求 |
| DELIVERY_GUARANTEE | String | 是 | 投递保障，如 至少一次、恰好一次 |
| CONSUMER_PATTERNS | String | 是 | 消费模式，如 广播、竞争消费、重试 |
| FAILURE_SCENARIOS | String | 否 | 需要重点处理的失败场景 |

## Prompt 正文

请基于以下信息，设计一套消息队列方案，并说明主题、消费和容错治理策略。

**业务场景：** {{BUSINESS_SCENARIO}}
**消息平台：** {{MESSAGE_PLATFORM}}
**吞吐要求：** {{THROUGHPUT_REQUIREMENTS}}
**投递保障：** {{DELIVERY_GUARANTEE}}
**消费模式：** {{CONSUMER_PATTERNS}}
**失败场景：** {{FAILURE_SCENARIOS}}

请完成以下任务：

1. 设计主题、队列、分区、键和消息体结构。
2. 说明生产者、消费者、重试、幂等和顺序保证策略。
3. 给出消息堆积、重复消费、消费失败和回压控制方案。
4. 说明监控指标、告警、死信处理和容量规划。
5. 给出实施步骤与迁移建议。

## 约束条件

1. 必须根据业务语义设计消息模型和消费语义，不得只给出中间件名称而缺少落地方案。
2. 必须说明幂等、重试和顺序保证策略，禁止忽略重复消费和消息乱序风险。
3. 必须定义失败处理、死信或补偿机制，不得让异常消息无限重试。
4. 必须说明关键指标与容量规划方法，禁止只设计功能不考虑运行稳定性。
5. 必须说明消息体版本演进与兼容策略，不得破坏上下游兼容性。

## 输出要求

1. 输出消息模型与拓扑设计。
2. 输出生产消费流程与可靠性策略。
3. 输出监控、告警与故障治理方案。
4. 输出容量规划与演进建议。
5. 输出示例消息结构。

6. 至少输出 1 个矩阵表、1 个流程图（可用 Mermaid 或等价文本流程）和 1 个风险清单（含风险、影响、缓解措施）。

## 输出格式

```markdown
# 消息队列设计方案

## 1. 业务背景与目标

## 2. 主题与消息模型
| 主题/队列 | 分区策略 | 生产者 | 消费者 | 投递语义 |
| ----------- | ---------- | -------- | -------- | ---------- |`r`n`r`n## 3. 可靠性与容错设计

## 4. 监控与容量规划

## 5. 示例消息体
```json
{}
`

## 附录 A. 消息与处理矩阵

| 主题/任务 | 生产者/触发方 | 消费者/执行方 | 可靠性策略 | 补偿或死信策略 |
| ------ | ------ | ------ | ------ | ------ |

## 附录 B. 流程图

（使用 Mermaid 或等价文本流程，至少包含：消息产生 -> 投递/调度 -> 消费执行 -> 成功确认或重试/DLQ）

## 附录 C. 风险清单

| 风险 | 触发场景 | 影响 | 缓解措施 | 补偿方式 |
| ------ | ------ | ------ | ------ | ------ |

## 自检清单

- [ ] 是否说明了消息模型与主题规划
- [ ] 是否覆盖了幂等、重试和顺序策略
- [ ] 是否设计了死信或补偿机制
- [ ] 是否给出了监控与容量规划方法
- [ ] 是否考虑了消息版本兼容性
